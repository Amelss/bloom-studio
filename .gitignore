node_modules
dist
dist-ssr
*.local
.DS_Store
coverage
.env
asset-work

# Local-only: Claude Code config + internal docs (never publish)
.claude
docs
public/flowers/*.png

# Supabase schema/migrations — kept local, not published to the repo
supabase/
*.env
.env.*
!.env.example
